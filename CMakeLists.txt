file(GLOB SOURCES images/*.c *.cpp)

set(INCLUDES
        ${CMAKE_CURRENT_SOURCE_DIR}
        ${CMAKE_CURRENT_SOURCE_DIR}/settings
)

set(ESPresso-UI-INCLUDES ${INCLUDES} PARENT_SCOPE)
set(ESPresso-UI-SOURCE ${SOURCES} PARENT_SCOPE)
